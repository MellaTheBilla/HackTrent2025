# Gym Scheduler

A full-stack gym scheduler app:

- **Frontend:** React + Material-UI (MUI)  
- **Backend:** FastAPI + YOLO for gym equipment detection

Users can upload equipment images, select available days and times, and generate a personalized workout schedule.

---

## Features

- Upload gym equipment images
- Select available days & time slots
- Generate workout schedule with:
  - Exercises
  - Muscle groups
  - Equipment used
- Dark-themed responsive UI
- Schedule table with time, equipment, and exercises
- Color-coded by muscle group

---

## Folder Structure

gym-scheduler/
├── frontend/ # React frontend
│ ├── src/
│ │ └── App.jsx
│ ├── package.json
│ └── .gitignore
├── backend/ # FastAPI backend
│ ├── main.py
│ ├── model.py
│ ├── weights/ # YOLO weights
│ ├── requirements.txt
│ └── .gitignore
└── README.md


---

## Frontend Setup

1. Navigate to the frontend folder:

```bash
cd frontend

    Install dependencies:

npm install

    Start the development server:

npm start

    Open http://localhost:3000

    in your browser.

Dependencies

    react

    axios

    @mui/material

    @mui/icons-material

    @mui/x-date-pickers

    dayjs

Backend Setup

    Navigate to the backend folder:

cd backend

    Install dependencies:

pip install -r requirements.txt

    Run the FastAPI server:

uvicorn main:app --reload

    Backend will run at http://localhost:8000

Backend API

POST /generate_plan

Request Form Data:

    images: List of uploaded equipment images

    days: List of days

    start_times: List of start times for each day

    end_times: List of end times for each day

Response Example:

{
  "plan": [
    {
      "day": "Monday",
      "start_time": "08:00",
      "end_time": "09:00",
      "equipment": "Dumbbells",
      "muscle_group": "Chest",
      "exercises": ["Bench Press", "Push-Ups"]
    }
  ]
}

Notes

    CORS is enabled for http://localhost:3000 by default.

    YOLO weights stored in backend/weights/best.pt.

    Schedule table is color-coded by muscle group.

    Images are optional; if none are uploaded, a default workout plan is generated.